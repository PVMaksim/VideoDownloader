# MEMORY.md — VideoGrab Backend

## Последняя сессия: 2026-05-02
### Сделано
- Исправлен порт локального API на 8010 (избегание конфликта с другими проектами)
- Перемещён `.env` в папку `backend/`, синхронизированы пути в `docker-compose.yml`
- Решена проблема авторизации PostgreSQL через полную очистку тома (`docker compose down -v`)
- Успешно запущены все сервисы: API, Celery Worker, PostgreSQL, Redis
- Проверен эндпоинт `/api/health` — возвращает `{"status":"ok","version":"2.0.0"}`

### Проблемы / Баги
- [ ] Ворнинг `The "vL5" variable is not set` при запуске compose
- [ ] Healthcheck PostgreSQL логирует ошибку `role "-d" does not exist` (ошибка в синтаксисе команды проверки в compose)

### Принятые решения
- Для локальной разработки зафиксирован порт 8010
- `.env` хранится в директории `backend/`, так как `docker-compose.yml` работает из этого контекста
- При смене паролей БД используется `down -v` для сброса состояния первичной инициализации

## Следующая сессия
- [ ] Исправить команду healthcheck PostgreSQL в `docker-compose.yml`
- [ ] Убрать/заглушить ворнинг переменной `vL5`
- [ ] Реализовать логику `SKIP_EMAIL_VERIFICATION` в модуле аутентификации
- [ ] Настроить реальные задачи Celery для скачивания видео
- [ ] Добавить интеграционные тесты

## Известный технический долг
- Жёсткая привязка к `bcrypt==3.2.2` из-за конфликта API с `passlib`
- Временный рабочий процесс с ручным сбросом тома БД при изменении учётных данных
- `src/auth/service.py` требует планового рефакторинга после серии экстренных правок