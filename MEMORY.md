# MEMORY.md — VideoGrab Backend

## Последняя сессия: 2026-05-29
### Сделано
- ✅ Настроена отправка писем через Resend API
- ✅ Исправлена ошибка `bcrypt: password > 72 bytes` (временная заглушка для тестов)
- ✅ Починен роутинг: `/auth/register` работает на порту 8201
- ✅ Получено успешное письмо с подтверждением на `m008ba@mail.ru`
- ✅ Добавлены отладочные логи (`🚀 [MARKER]`) для диагностики

### Проблемы / Баги
- [ ] Хеширование паролей: заглушка `dummy_hash_` вместо `bcrypt` (нужно вернуть)
- [ ] Ограничение Resend: отправка только на верифицированный email (пока `m008ba@mail.ru`)
- [ ] Ссылки в письмах ведут на `localhost` (нужен туннель или реальный домен)

### Принятые решения
- Использовать `bcrypt__ident="2b"` + обрезку пароля до 72 байт для обхода бага passlib
- Для тестов: `SKIP_EMAIL_VERIFICATION=true` или отправка на свой email
- Для продакшена: верифицировать домен в Resend, настроить `APP_URL`

## Следующая сессия
- [ ] Вернуть нормальное хеширование паролей (`bcrypt` с обрезкой)
- [ ] Протестировать скачивание файла (отключив временно регистрацию)
- [ ] Настроить туннель (ngrok/localhost.run) для публичного доступа
- [ ] Подготовить деплой на сервер (VPS)

## Известный технический долг
- В `service.py`: заглушка `hash_password` → вернуть `bcrypt` с `pwd_bytes[:72]`
- В `email.py`: жестко закодированный `EMAIL_FROM` → вынести в конфиг
- В `router.py`: дублирование схем (`RegisterRequest`) → импортировать из `schemas/`