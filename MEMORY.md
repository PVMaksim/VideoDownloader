# MEMORY.md — VideoGrab Backend

## Последняя сессия: 2026-05-01
### Сделано
- Настроен CI/CD пайплайн через GitHub Actions для автодеплоя на VPS
- Исправлены миграции Alembic (неблокирующий запуск, корректные пути)
- Добавлена валидация email (`email-validator` для Pydantic)
- Исправлена совместимость `passlib` и `bcrypt` (зафиксирована версия `bcrypt==3.2.2`)
- Реализовано безопасное усечение пароля до 72 байт в `hash_password()`
- Протестирован полный цикл регистрации и логина (JWT выдаётся успешно)
- Удалён устаревший атрибут `version` из `docker-compose.yml`

### Проблемы / Баги
- [ ] Флаг `SKIP_EMAIL_VERIFICATION` добавлен локально в `.env`, но не реализован в коде
- [ ] Ворнинги в docker-compose: переменная `vL5` не задана

### Принятые решения
- Использовать `bcrypt==3.2.2` из-за конфликта API в новых версиях с `passlib`
- Деплой через `git reset --hard origin/main` + `docker compose build`
- Alembic запускается в фоне (`alembic upgrade head &`) чтобы не блокировать `uvicorn`

## Следующая сессия
- [ ] Реализовать обработку `SKIP_EMAIL_VERIFICATION` в коде (для dev-режима)
- [ ] Убрать предупреждения docker-compose (`vL5`)
- [ ] Настроить реальные email-рассылки через Resend
- [ ] Добавить эндпоинты для загрузки/скачивания видео
- [ ] Написать интеграционные тесты

## Известный технический долг
- `src/auth/service.py` требует рефакторинга после нескольких экстренных правок
- Жёсткая привязка к версии `bcrypt` — в перспективе рассмотреть `argon2` или явный выбор бэкенда в `passlib`